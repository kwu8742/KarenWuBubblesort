package com.company;

public class BubbleSort {
    public static void bubbleSort(int arr[]){
        int i = 0;
        int j = 1;
        while(i < array.length())
        if (arr[i] > arr[j]) {
            int x = arr[i];
            arr[i] = arr[j];
            arr[j] = x;
            i++;
            j++;
        }
    }
}
